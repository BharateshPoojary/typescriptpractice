let rollNo :Array<number>=[]//here we are defining an array of type number 
rollNo.push(12)
let names:string[]=[]//here we are defining an array of type string
names.push("")

type userCreds={
    _id:number;
    name:string
}
let userCredsArray:userCreds[]=[//here we are defining an array of type userCreds 
    {_id:101,name:"bharat"}//like this we can pass the value to it or using .push({..}) method
]

let matrix :number[][]=[//here we are defining an array inside array for cases like matrix operation etc

]
matrix.push([101,102])
