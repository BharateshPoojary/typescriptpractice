enum seatChoice{
    FIRST ="first",
    SECOND="second",
    THIRD="third",
    FOURTH="fourth"
}
const seatchoice = seatChoice.FIRST;//so here  seatChoice.FIRST will return  1 if not mentioned then default value will be returned that is 0
//Now this will return first we cab define custom value like this or else it will return 0